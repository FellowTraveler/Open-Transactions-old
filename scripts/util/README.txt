 NOTE: still need to update this script....
(It won't work right now.)

Although the code has now been improved to the point where you shouldn't get out 
of sync, here is a script (resync.ot) that will re-synchronize your Nym if he 
ever somehow misses a server reply.

