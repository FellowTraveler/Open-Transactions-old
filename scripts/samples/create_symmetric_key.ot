#!/usr/bin/env ot

	var strKey = OT_API_CreateSymmetricKey()
	 
	if (VerifyStringVal(strKey))
	{
		// stderr
		OT_API_Output(0, "\n")

		// stdout
		print(strKey)

		// stderr
		OT_API_Output(0, "\n")
		
		return 1
    }

// "return value"
0
