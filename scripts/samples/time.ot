#!/usr/bin/env ot

var current_time = OT_API_GetTime()

OT_API_Output(0, "The current time (in seconds) is: " + current_time + "\n" )

// Without the below '1', we'd see a boxed_cast exception, 
// since OT expects a return value.

1

